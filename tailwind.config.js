import { blackA, iris, irisA, irisDarkA } from "@radix-ui/colors";

export default {
  theme: {
    extend: {
      colors: {
        ...blackA,
        ...iris,
        ...irisA,
        ...irisDarkA,
      }
    }
  }
};
